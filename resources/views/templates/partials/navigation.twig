<div id="nav">
	<div class="left"><a href="#"><img src="{{ base_url() }}/img/platform/logo.png" height="15px"></a></div>
	<div class="right">
		<a href="#">Documentation</a>
		<a href="#">Browse Packages</a>
		<!--
		{% if auth.check %}
			<a href="#">Profile</a>
			<a href="{{ path_for('auth.signout') }}">Sign out</a>
		{% else %}
			<a href="{{ path_for('auth.signin') }}">Sign in</a>
			<a href="{{ path_for('auth.signup') }}">Sign up</a>
		{% endif %} -->
	</div>
	<div class="clear"></div>
</div>